Static scan scope: tp, sl, stop, take, upper, lower, short, long, side, Buy, Sell, reduceOnly, kill, leverage, pnl, roi, risk, operator_sheet, sizing, notional, margin.

Reviewed focus vs recent scans:
- app/trading_semantics.py remains the canonical directional source of truth for long/short/neutral TP/SL, gross PnL, risk/reward and Bybit one-way side/reduceOnly/closeOnTrigger semantics. No new bypass was introduced.
- app/main.py execution/preflight validation already reads params.operator_sheet.* for Bybit sizing/economics checks, but _execution_runtime_size_risk_blocks did not read operator_sheet leverage, sizing, economics or risk_report. This was unsafe because runtime max leverage/notional/margin caps could be skipped for operator-sheet-only launch payloads.
- app/ui/static/app.js already reads operator_sheet.sizing/operator_sheet.economics/operator_sheet.leverage for displayed position math; the patch aligns runtime backend guards with that shipped UI/preflight convention.
- No changes were made to Bybit Buy/Sell mapping, reduceOnly, closeOnTrigger, triggerDirection, TP/SL geometry or frontend rendering.

Changed hits reviewed:
- app/main.py:_execution_runtime_size_risk_blocks — SAFE/HARDENING. Added operator_sheet maps to runtime risk inputs and operator_sheet.leverage as fallback after params/trade_plan leverage. This makes the guard stricter and fail-closed for operator-sheet-only payloads.
- tests/test_iteration181_operator_sheet_runtime_size_caps.py — SAFE. Red→green tests cover operator_sheet leverage/notional/margin cap enforcement and unpriced operator_sheet qty context.
